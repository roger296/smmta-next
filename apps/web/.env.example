# Copy to .env.local for local dev overrides
VITE_API_BASE_URL=http://localhost:3000/api/v1
# Sections to leave out of the menu, comma-separated section keys (a section's
# path without the slash; the first page is `dashboard`). Empty shows all.
VITE_HIDDEN_SECTIONS=
