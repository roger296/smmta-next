node_modules/
dist/
.turbo/
.env
.env.local
.env.*.local
*.tsbuildinfo
coverage/
.DS_Store

# Vendor / supplier materials (docs, sample data, CSV dumps) and
# local-only briefs. Kept on disk for reference; never committed.
.tmp.*/
.tmp-*
*.docx

# Shipping label PDFs written by a local run (LABELS_DIR defaults to ./labels).
# They contain customer names and addresses: never commit them.
labels/
apps/api/labels/
